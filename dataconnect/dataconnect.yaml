specVersion: "v1beta"
serviceId: "workouttracker"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "workouttracker-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
